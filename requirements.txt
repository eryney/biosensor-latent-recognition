# Python dependencies for the analysis and figure scripts.
# Tested with Python 3.11+. Versions below are lower bounds known to work;
# newer releases are generally fine.
numpy>=1.26
pandas>=2.1
matplotlib>=3.8
rdkit>=2023.9
openpyxl>=3.1
pillow>=10.0

# The Figure 1 panel scripts additionally require PyMOL, which is not needed to
# regenerate Figures 2-4 or the supplementary tables:
#   pymol-open-source>=2.5
